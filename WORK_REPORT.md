# 작업 보고서: 데이터 마이그레이션 안정성 강화

## 1. 문제점 분석

최초 코드 분석 결과, 데이터 동기화 프로세스에서 두 가지 심각한 문제가 발견되었습니다.

### 문제 1: 오류 발생 시 데이터 누락 (초기 문제)

- **현상**: 소스 DB에서 읽어온 데이터 배치를 타겟 DB에 쓰는 과정에서 일부 레코드가 실패하더라도, 마지막 동기화 시간(`last_sync_time`)은 성공 여부와 관계없이 가장 최신 데이터의 시간으로 업데이트되었습니다.
- **영향**: 이로 인해 쓰기 실패한 레코드는 다음 동기화 주기에서 건너뛰게 되어 영구적으로 데이터가 누락되는 문제가 발생했습니다.

### 문제 2: 영구 오류 발생 시 무한 재시도 (추가 발견)

- **현상**: 위 문제 1을 해결하기 위해 '쓰기 실패 시 동기화 시간을 업데이트하지 않는' 로직을 적용했습니다. 하지만 이 경우, '파티션 부재'와 같이 절대 해결될 수 없는 영구적인 오류가 특정 데이터 배치에 포함되어 있다면, 해당 배치를 무한정 재시도하게 됩니다.
- **영향**: 특정 테이블의 동기화가 완전히 중단되어 더 이상 새로운 데이터가 동기화되지 않는 심각한 장애로 이어집니다.

## 2. 해결 방안: 서킷 브레이커 패턴 도입

위 두 가지 문제를 모두 해결하기 위해, 재시도 메커니즘과 '서킷 브레이커(Circuit Breaker)' 패턴을 결합한 안정성 강화 로직을 구현했습니다.

1.  **제한된 재시도 (Limited Retries)**
    - 데이터 동기화 실패 시, 즉시 건너뛰지 않고 다음 동기화 주기에서 재시도를 수행합니다. (문제 1 해결)

2.  **연속 실패 감지 (Consecutive Failure Detection)**
    - 각 테이블별로 연속 실패 횟수를 추적합니다. 동기화가 한 번이라도 성공하면 이 카운터는 0으로 초기화됩니다.

3.  **서킷 브레이커 작동 (Tripping the Breaker)**
    - 특정 테이블이 설정된 임계값(`MAX_CONSECUTIVE_FAILURES = 3`회) 이상 연속으로 실패하면 '서킷 브레이커'가 작동합니다.
    - **핵심 동작**:
        - 해당 테이블을 '영구 실패' 상태로 전환합니다.
        - 문제가 되는 데이터 구간을 **강제로 건너뛰기 위해** 마지막 동기화 시간을 현재 시간으로 업데이트합니다.
        - 관리자가 문제를 인지하고 조치할 수 있도록 대시보드에 '영구 실패' 상태와 관련 메시지를 명확히 표시합니다.

- **기대 효과**: 이 로직을 통해 일시적인 네트워크 문제 등은 재시도를 통해 자체 해결하고, 파티션 부재와 같은 영구적인 문제는 해당 데이터를 격리(건너뛰기)하여 전체 동기화 프로세스가 중단되는 것을 방지합니다.

## 3. 주요 구현 내용

### `realtime_sync_service.py`

- **상수 추가**: `MAX_CONSECUTIVE_FAILURES` (연속 실패 허용 횟수)를 3으로 정의했습니다.
- **상태 초기화**: 각 테이블의 상태(`cycle_status`)에 `consecutive_failures` 카운터를 추가했습니다.
- **`sync_single_table` 함수 로직 변경**:
    - 동기화 성공 시: `consecutive_failures`를 0으로 리셋합니다.
    - 오류 발생 시:
        - `consecutive_failures`를 1 증가시킵니다.
        - 카운터가 `MAX_CONSECUTIVE_FAILURES`에 도달하면, 서킷 브레이커를 작동시켜 동기화 시간을 강제로 업데이트하고 상태를 'persistent_failure'로 변경합니다.
        - 카운터가 임계값 미만이면, 기존 로직에 따라 동기화 시간을 업데이트하지 않고 다음 주기에 재시도를 유도합니다.

### `migration_utils.py`

- **`migrate` 함수 로직 변경**:
    - 데이터 배치 쓰기에 오류가 없을 때만 마지막 동기화 시간(`max_ts`)을 업데이트하도록 수정했습니다.
    - 이 변경은 서킷 브레이커가 작동하기 전, 일반적인 재시도 로직의 기반이 됩니다.

### `dashboard_generator.py`

- **CSS 스타일 추가**: '영구 실패' 상태를 명확히 구분하기 위한 `.status-persistent_failure` 스타일(빨간색 배경)을 추가했습니다.
- **상태 메시지 업데이트**:
    - '실패' 상태를 '일시적 실패'로 명칭을 변경했습니다.
    - 새로운 '영구 실패' 상태와 그에 대한 설명("반복적인 오류로 인해 특정 데이터 구간을 건너뛰었습니다...")을 대시보드에 추가했습니다.

## 4. 결론

이번 개선 작업을 통해 데이터 마이그레이션 서비스의 안정성과 가용성을 크게 향상했습니다. 이제 시스템은 일시적인 오류를 스스로 복구하고, 영구적인 오류가 발생하더라도 전체 서비스 중단 없이 운영될 수 있으며, 관리자는 대시보드를 통해 문제 상황을 신속하게 파악하고 대응할 수 있습니다.
