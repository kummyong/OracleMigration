# 작업 보고서: 데이터 마이그레이션 안정성 강화 및 성능 최적화

## 1. 문제점 분석

최초 코드 분석 결과, 데이터 동기화 프로세스에서 두 가지 심각한 문제가 발견되었습니다.

### 문제 1: 오류 발생 시 데이터 누락 (초기 문제)

- **현상**: 소스 DB에서 읽어온 데이터 배치를 타겟 DB에 적재하는 과정에서 일부 레코드가 실패하더라도, 마지막 동기화 시간(`last_sync_time`)이 성공 여부와 관계없이 가장 최신 데이터의 시간으로 업데이트되었습니다.
- **영향**: 이로 인해 적재 실패한 레코드는 다음 동기화 주기에서 건너뛰게 되어 영구적으로 데이터가 누락되는 문제가 발생했습니다.

### 문제 2: 영구 오류 발생 시 무한 재시도 (추가 발견)

- **현상**: 위 문제 1을 해결하기 위해 '적재 실패 시 동기화 시간을 업데이트하지 않는' 로직을 적용했습니다. 하지만 이 경우, '파티션 부족'과 같이 자동으로 해결될 수 없는 영구적인 오류가 특정 데이터 배치에 포함되어 있다면, 해당 배치를 무한히 재시도하게 됩니다.
- **영향**: 특정 테이블의 동기화가 완전히 중단되어 더 이상 새로운 데이터가 동기화되지 않는 심각한 장애로 이어집니다.

### 문제 3: Source DB 조회 성능 병목 (신규 발견)

- **현상**: `migration_utils.py`에서 수행하는 Source DB 조회 쿼리(`SELECT * FROM ... WHERE date_col >= :last_sync`)가 인덱스를 효율적으로 타지 못해 병목이 발생하고 있습니다.
- **요청사항**: Source Table의 인덱스 컬럼을 활용하여 조회 성능을 개선하자는 제안이 있었습니다. 단, 인덱스 컬럼과 실제 동기화 기준 컬럼(Business Date) 간에 시간 차이가 존재합니다.

## 2. 해결 방안: 서킷 브레이커 패턴 도입 및 쿼리 최적화

위 세 가지 문제를 모두 해결하기 위해, 재시도 메커니즘과 '서킷 브레이커(Circuit Breaker)' 패턴을 결합하고, 조회 쿼리를 개선하는 방안을 수립했습니다.

### A. 안정성 강화 (서킷 브레이커)

1.  **제한된 재시도 (Limited Retries)**
    - 데이터 동기화 실패 시 즉시 건너뛰지 않고 다음 동기화 주기에서 재시도를 수행합니다. (문제 1 해결)

2.  **연속 실패 감지 (Consecutive Failure Detection)**
    - 각 테이블별로 연속 실패 횟수를 추적합니다. 동기화가 한 번이라도 성공하면 이 카운터는 0으로 초기화됩니다.

3.  **서킷 브레이커 작동 (Tripping the Breaker)**
    - 특정 테이블이 설정된 임계값 `MAX_CONSECUTIVE_FAILURES = 3` 이상 연속으로 실패하면 '서킷 브레이커'가 작동합니다.
    - **핵심 동작**:
        - 해당 테이블을 '영구 실패' 상태로 전환합니다.
        - 문제가 되는 데이터 구간을 **강제로 건너뛰기 위해** 마지막 동기화 시간을 현재 시간으로 업데이트합니다.
        - 관리자가 문제를 인지하고 조치할 수 있도록 대시보드에 '영구 실패' 상태와 관련 메시지를 명확히 표시합니다.

### B. 성능 최적화 (인덱스 힌트 활용)

1.  **보조 인덱스 컬럼 활용 (Index Hinting)**
    - `tables_config.json`에 `hint_index_column` (인덱스 컬럼명) 설정을 추가합니다.
    - 동기화 기준 컬럼(`date_column`) 외에, 인덱스가 걸려 있는 컬럼을 WHERE 절에 추가하여 검색 범위를 좁힙니다.

2.  **시간 차이(Drift) 보정**
    - 인덱스 컬럼과 동기화 기준 컬럼 간의 시간 차이를 보정하기 위해 `index_scan_gap_minutes` 설정을 추가합니다.
    - **쿼리 전략**:
      ```sql
      SELECT * FROM table
      WHERE {hint_index_column} >= :start_date - INTERVAL 'gap' MINUTE
        AND {date_column} >= :start_date
        AND {date_column} < :end_date
      ```
    - 이를 통해 인덱스를 타면서도, 시간 차이로 인해 데이터가 누락되는 것을 방지합니다.

### C. 추가 검토 사항 (인덱스 미존재 및 ORA-01461)

1.  **인덱스가 없는 케이스 검토**
    - **검토 내용**: 설정 파일에 `hint_index_column`이 누락되거나 `None`인 경우에 대한 안정성 검토.
    - **결과**: `migration_utils.py` 로직 내에서 `if hint_index_column:` 조건문을 통해 인덱스 컬럼이 제공된 경우에만 추가 `WHERE` 절을 생성하도록 구현되어 있습니다. 따라서 설정이 없으면 자동으로 기존 방식(Full Scan 또는 Date Column Range Scan)으로 동작하므로 **별도의 코드 수정 없이 안전하게 처리됩니다.**

2.  **ORA-01461 오류 검토 및 해결**
    - **현상**: 문자열 데이터가 2000~4000 바이트 사이일 때 `cx_Oracle`에서 이를 `LONG` 타입으로 바인딩하려고 시도하다가, 타겟 컬럼이 `VARCHAR2`인 경우 "ORA-01461: can bind a LONG value only for insert into a LONG column" 오류가 발생할 수 있음이 확인되었습니다.
    - **조치**: `migration_utils.py`의 `_load_data_merge` 함수 내에서 문자열 길이에 따라 `DB_TYPE_LONG`으로 강제 변환하던 로직을 제거하고, **모든 문자열을 `DB_TYPE_VARCHAR`로 바인딩**하도록 수정했습니다. 이를 통해 Oracle 드라이버와 DB가 적절한 타입을 자동으로 협상하도록 하여 오류를 방지했습니다.

## 3. 주요 구현 내용

### `realtime_sync_service.py`

- **상수 추가**: `MAX_CONSECUTIVE_FAILURES` (연속 실패 허용 횟수)를 3으로 정의했습니다.
- **상태 초기화**: 각 테이블의 상태(`cycle_status`)에 `consecutive_failures` 카운터를 추가했습니다.
- **`sync_single_table` 함수 로직 변경**:
    - 동기화 성공 시 `consecutive_failures`를 0으로 리셋합니다.
    - 오류 발생 시:
        - `consecutive_failures`를 1 증가시킵니다.
        - 카운터가 `MAX_CONSECUTIVE_FAILURES`에 도달하면, 서킷 브레이커를 작동시켜 동기화 시간을 강제로 업데이트하고 상태를 'persistent_failure'로 변경합니다.
        - 카운터가 임계값 미만이면, 기존 로직에 따라 동기화 시간을 업데이트하지 않고 다음 주기에 재시도를 유도합니다.

### `migration_utils.py`

- **`migrate` 함수 로직 변경**:
    - 데이터 배치 적재에 오류가 없을 때만 마지막 동기화 시간(`max_ts`)을 반환하도록 수정했습니다.
    - `hint_index_column` 및 `time_gap` 파라미터를 받아 최적화된 쿼리를 생성하도록 개선했습니다.
    - **ORA-01461 수정**: `setinputsizes` 설정 시 문자열에 대한 불필요한 `LONG` 타입 강제 로직을 제거했습니다.

### `dashboard_generator.py`

- **CSS 스타일 추가**: '영구 실패' 상태를 명확히 구분하기 위한 `.status-persistent_failure` 스타일(빨간색 배경)을 추가했습니다.
- **상태 메시지 업데이트**:
    - '실패' 상태를 '일시적 실패'로 명칭을 변경했습니다.
    - 새로운 '영구 실패' 상태와 그에 따른 설명("반복적인 오류로 인해 특정 데이터 구간을 건너뛰었습니다..")을 대시보드에 추가했습니다.

## 4. 결론 및 향후 계획

이번 개선 작업을 통해 데이터 마이그레이션 서비스의 안정성과 가용성이 크게 향상되었습니다. 이제 시스템은 일시적인 오류를 스스로 복구하고, 영구적인 오류가 발생하더라도 전체 서비스 중단 없이 운영될 수 있으며, 관리자는 대시보드를 통해 문제 상황을 신속하게 파악하고 대응할 수 있습니다.
또한, 인덱스 힌트를 통한 조회 성능 최적화와 잠재적인 ORA-01461 오류에 대한 방어 로직까지 적용되어 더욱 견고한 시스템이 되었습니다.
