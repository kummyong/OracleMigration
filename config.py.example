# config.py.example
import os

# 스크립트 위치 기준 절대 경로 설정
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# Source DB 설정 (예시)
SOURCE_DB_CONFIG = {
    'user': 'YOUR_USER',
    'password': 'YOUR_PASSWORD',
    'dsn': 'localhost:1521/XE'
}

# Target DB 설정 (예시)
TARGET_DB_CONFIG = {
    'user': 'YOUR_USER',
    'password': 'YOUR_PASSWORD',
    'dsn': 'localhost:1522/XE'
}

# 설정 파일 경로
TABLES_CONFIG_FILE = os.path.join(BASE_DIR, 'tables_config.json')
DASHBOARD_FILE = os.path.join(BASE_DIR, 'dashboard.html')
LAST_SYNC_TIME_FILE = os.path.join(BASE_DIR, 'last_sync.json')
