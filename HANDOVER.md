# 프로젝트 인수인계서 (Handover Note)

## 1. 프로젝트 개요
Oracle DB 간의 실시간 데이터 동기화를 위한 마이그레이션 서비스입니다. 대용량 데이터 처리를 위한 자율 주행 청킹(Adaptive Chunking)과 안정성을 위한 서킷 브레이커 패턴이 적용되어 있습니다.

## 2. 주요 수정 및 조치 사항 (2026-02-27)

### A. 시스템 기반 구축
- **`config.py` 생성**: 누락되었던 핵심 설정 파일을 생성했습니다. DB 연결 정보 및 파일 경로를 중앙 관리합니다.
- **의존성 최신화**: `requirements.txt`에 `oracledb`를 추가하여 Oracle Instant Client 설치 없이도 'Thin 모드'로 작동할 수 있도록 개선했습니다.

### B. 안정성 및 호환성 강화
- **스키마 포함 테이블 처리**: `migration_utils.py`에 `get_quoted_table` 함수를 도입하여 `USER.TABLE_NAME` 형식의 테이블도 `"USER"."TABLE_NAME"`으로 올바르게 쿼팅되도록 수정했습니다.
- **ORA-01461 오류 방지**: `migration_utils.py`에서 4000바이트 이상의 문자열에 대해 불필요하게 `CLOB`으로 강제 바인딩하던 로직을 제거했습니다. Oracle 드라이버가 자동으로 타입을 협상하도록 하여 `VARCHAR2` 컬럼 적재 시의 오류 가능성을 줄였습니다.

### D. 서비스 통합 제어 및 주기 설정
- **전체 시작/중지 기능**: 대시보드 상단에 [전체 시작], [전체 중지] 버튼을 추가했습니다.
    - **시작 버튼 클릭 시**: 서비스가 활성화되어 동기화 사이클이 돌아갑니다.
    - **중지 버튼 클릭 시**: 현재 진행 중인 사이클이 완료된 후 다음 사이클부터는 대기 상태로 유지됩니다.
- **동기화 주기 가변 설정**: TextBox를 통해 동기화 주기(초)를 실시간으로 변경할 수 있습니다. 변경된 주기는 즉시 `service_config.json`에 저장되어 영구 적용됩니다.
- **설정 영구 저장**: 전체 서비스 실행 상태와 동기화 주기는 `service_config.json` 파일을 통해 관리되어 서비스 재시작 시에도 이전 상태가 유지됩니다.

## 3. 현재 상태 및 실행 방법

### 실행 방법
1. `.venv` 활성화: `.\.venv\Scripts\activate`
2. 패키지 설치: `pip install -r requirements.txt`
3. 서비스 실행: `python realtime_sync_service.py`
4. 대시보드 확인: 브라우저에서 `dashboard.html` 열기 (또는 `http://localhost:8080` 접속)

### 대시보드 제어
- 각 테이블 행의 **[중지] / [재개]** 버튼을 통해 실시간으로 동기화 여부를 제어할 수 있습니다.
- 제어 명령은 다음 동기화 사이클(기본 30초)부터 즉시 적용됩니다.

## 4. 향후 작업 권장 사항 (To-do)
- **테스트 자동화**: `tests/integration_test.py`가 현재 환경(Docker 등)에서 정상 작동하는지 재검증이 필요합니다.
- **자율 주행 청킹 최적화**: 현재 5초 목표로 설정된 `ideal_fetchsize` 로직을 네트워크 환경에 맞춰 미세 조정할 필요가 있습니다.
- **로깅 강화**: 영구 실패(`persistent_failure`) 발생 시 관리자에게 알림(Email, Slack 등)을 보내는 기능을 검토하십시오.

---
**작성자:** Gemini CLI Agent
**날짜:** 2026년 2월 27일
